<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <h1>Message Board</h1>
    <% for(let i = 0; i<messages.length; i++){ %>
        <h2><%= messages[i].text %></h2>
        <h3><%= messages[i].user %></h3>
        <h3><%= messages[i].added %></h3>
        <a href = "<%= 'display/' + i %>">open message</a>
    <% } %>
    <a href = "/new">New Message!</a>
</body>
</html>